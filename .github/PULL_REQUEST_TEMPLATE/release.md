<!--
This body is published verbatim as the release notes. `.github/workflows/release.yml`
writes it to a file and hands it to `gh release create --notes-file`, so everything
left here reaches the notes, including this comment if it is not deleted.

Members read it, not a reviewer. So it carries no checkbox, no task list and
nothing addressed to whoever merges it.

It is written as notes rather than as bullets of fact. Prose, in the past tense,
about what the association's app does now that it did not before. Say whether the
release carries a migration in a sentence.

No em dashes and no colons. No emoji. Nothing read from the production database.

Title it `Release X.Y.Z` exactly, or the workflow refuses it.

Read the previous release for the shape. Delete this comment.
-->

## What changed

## Why

## How it was tested
