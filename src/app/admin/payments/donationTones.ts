import { RISKY, SAFE } from "@/components/admin/verbTones";

export const PRIMARY = { background: "var(--mint-600)", color: "white" };
export const DANGER = RISKY;
export const QUIET = SAFE;
export const FIELD = { backgroundColor: "white" };
